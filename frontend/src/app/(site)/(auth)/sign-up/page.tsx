import SignUp from "@/app/components/auth/sign-up";
import { Metadata } from "next";

export const metadata: Metadata = {
  title:
    "Sign Up | BlogForge Sanity Blog Template",
};

const SignIn = () => {
  return (
    <SignUp/>
  )
}

export default SignIn